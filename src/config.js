const DB_CONFIG = {
    host: 'localhost',
    user: 'root',
    password: '990301aa'
}

const appConfig = {
    env: process.env.NODE_ENV,
    DB_CONFIG: {
        host: 'localhost',
        user: 'root',
        password: '990301aa'
    },
    port: 3000
}

module.export  = DB_CONFIG